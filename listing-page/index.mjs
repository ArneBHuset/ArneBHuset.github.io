import {
  displayListingCards,
  validatedNewListing,
  newListingDropdown,
  headerDropdown,
  headerLoginStatus,
  currentActiveFilter,
  currentSearchInput,
  updateFilteredListingCards,
  updateSearchFilterDisplay,
  defaultTimeNewListing,
} from '../src/main.mjs';

//
displayListingCards();

validatedNewListing();

newListingDropdown();

headerDropdown();

headerLoginStatus();

currentActiveFilter();
currentSearchInput();
updateFilteredListingCards();
updateSearchFilterDisplay();

defaultTimeNewListing();
