import {
  displayListingCards,
  validatedNewListing,
  newListingDropdown,
  headerDropdown,
  headerLoginStatus,
  currentActiveFilter,
  currentSearchInput,
  updateFilteredListingCards,
  updateSearchFilterDisplay,
  defaultTimeNewListing,
  searchForListing,
  eventListener,
  checkBoxPosition,
} from '../src/main.mjs';

//
displayListingCards();

validatedNewListing();

newListingDropdown();

headerDropdown();

headerLoginStatus();

currentActiveFilter();
currentSearchInput();
updateFilteredListingCards();
updateSearchFilterDisplay();

defaultTimeNewListing();

searchForListing();
eventListener();
checkBoxPosition();
